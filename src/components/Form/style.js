import { StyleSheet } from  'react-native'
const style = StyleSheet.create({
 formContent:{
   width: '100%',
   height: '100%',
   bottom: 0,
   backgroundColor: "#fff",
   alignItems: 'center',
   marginTop: 30,
   borderRadius: 15
   

 }
})

export default style